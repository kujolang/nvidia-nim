# Agent Guidance

- Native entrypoint: `src/nvidia_nim.kujo`; driver: `src/provider.kujo`.
- Preserve hosted/self-managed endpoint and model semantics.
- Keep the compatible driver pure; do not add NVIDIA branches to AI SDK core.
